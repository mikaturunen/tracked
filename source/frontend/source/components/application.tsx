import * as React from 'react';
import LoginWindow from '../containers/login-window';

const Application = () => (
  <div>
    <LoginWindow username='' password='' />
  </div>
)

export default Application;
